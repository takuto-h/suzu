
module Ref:
  class C = make:
    mutable value
  end

  module Open:
    let C#value = C#value
    let C#(value=) = C#(value=)

    def C#(==)(self, other):
      self.value == other.value
    end

    export C#value, C#(value=), C#(==)
  end

  export make, C, Open
end

open Ref::Open
